import { motion } from 'framer-motion';
interface ContactHeaderProps {
  title: string;
  subtitle: string;
  description: string;
}
const ContactHeader = ({
  title,
  subtitle,
  description
}: ContactHeaderProps) => {
  return;
};
export default ContactHeader;